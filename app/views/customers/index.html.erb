
<h1 class='title'>Kundendaten</h1> 

<table class='table'>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>  
  </thead>
  
  <tbody>  
    <% @customers.each do |customer| %>
      <tr>
        <td><%= customer.name %></td>
        <td><%= link_to 'Bearbeiten', edit_customer_path(id: customer) %></td>
        <td><%= link_to 'Löschen', customer_path(id: customer), method: :delete, data: { confirm: 'Sind Sie sich sicher?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>      
</br>
<%= link_to 'Neuen Kunden Anlegen', new_customer_path %>  

